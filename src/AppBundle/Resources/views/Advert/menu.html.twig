
{#Template qui n'hérite pas, comme 'include'#}

<ul class="nav nav-pills nav-stacked">
    {% for advert in listAdverts%}
        <li>
            <a href="{{ path('platform_view', {'id':advert.id}) }}">
                {{ advert.title }}
            </a>
        </li>
    {% endfor %}
</ul>